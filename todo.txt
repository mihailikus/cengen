Приделать исполнение сценариев

Приделать в редакторе шаблонов количество товара!!!

FIX. Сделать перенос строк, если сочетаются два служебных символа

FIX. Добавить управление лимитом поиска по названию

FIX. В вызове рендеринга штрих-кода не используются функция задания отступов -
    сделать такую фичу в редакторе шаблонов и в классе Ценник

FIX. Переделать формат шаблона ценников на XML

FIXING. Сделать редактор шаблона ценников

FIX. Добавить масштабирование ценников (чтоб делать маленькие)

FIXed for TOP. Сделать отступы от полей страницы

FIX. Сделать возможность выбора каждому ценнику своего шаблона из найденных в текущем
    каталоге

Приделать верхний и нижний колонтитул (на основе шаблонов и отступов от полей)

Приделать нумерацию страниц

Переделать функцию поиска в DBF-файле на многопоточность

FIX. Сделать основное окно номально растягивающимся

Отображение имени файла в окне
При выходе - вопрос о сохранении несохраненного списка

FIX. Сделать, чтобы основная форма забирала имя шаблона из редактора при открытии/сохранении

Сделать хранение в QSettings всего XML-кода шаблона, а не только имени файла

FIX. Редактор падает при нажатии кнопок вправо-влево, если не выбран объект.
Сделать:
    - FIX. не выделен объект - ничего не делать
    - FIXING. если выделено два и более объектов - выровнять их по соотв. границе
    - если выделен один, запомнить перемещение, после этого нажатие центральной кнопки
            означает центровку объекта по соотв. оси
                (т.е. если нажали вверх, затем центр - центруем по горизонтали)

FIX. Сделать, чтоб объект Ценник создавался один раз, а потом отрисовывался по-разному,
    а то ж тормозит

Сделать модуль qsettings отдельным классом

Затем сделать, чтоб имя организации запрашивалось перед первым стартом программы
    в отдельном окне, а затем сохранялось в файл. И если файл есть - читать оттуда

FIX. Сделать центральную таблицу отдельным классом

FIX. Сделать список полей для центральной таблицы, чтоб можно было выбрать, какие поля
    будут отображены в центральной таблице

FIX. Сделать в фильтре - обновление (по запросу) найденного поля в таблицу
    (например, количество - заносить в центральную таблицу)

FIX. Да и вообще, сделать, чтоб этот фильтр работал

FIX. Разобраться с функцией "Вычитание списка товаров"

Сделать тестовую базу данных для выгрузки на общедоступный сервер

Сделать тестовые шаблоны для размещения на сервере

В конце концов, собрать MySQL для виндоус
